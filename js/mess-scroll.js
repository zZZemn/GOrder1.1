// Store the initial scroll height
var initialScrollHeight = $(".message-show")[0].scrollHeight;

// Scroll to the bottom of the container on page load
$(".message-show").scrollTop(initialScrollHeight);